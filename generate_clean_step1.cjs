const fs = require('fs');
const path = require('path');

// Let's create a script that generates the complete source code directly and compiles it on the server
// The files are very clean and readable.
